export interface ShiyeTierThresholdSettings {
  competition_low_percentile: number;
  competition_high_percentile: number;
  score_low_percentile: number;
  score_high_percentile: number;
  stable_min_score: number;
  sprint_min_score: number;
}
