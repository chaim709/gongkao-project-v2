services:
  db:
    image: postgres:16-alpine
    container_name: gongkao-guanli-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gongkao_db}
      POSTGRES_USER: ${POSTGRES_USER:-gongkao_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gongkao_pass}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gongkao_user}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: gongkao-guanli-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-gongkao_user}:${POSTGRES_PASSWORD:-gongkao_pass}@db:5432/${POSTGRES_DB:-gongkao_db}
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY environment variable is required}
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 120
      DEBUG: "false"
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost}
    volumes:
      - uploads:/app/uploads
    ports:
      - "8001:8000"

  frontend:
    build: ./frontend
    container_name: gongkao-guanli-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "8888:80"

volumes:
  pgdata:
  uploads:
