# 公考管理系统 - 定时任务配置
# 安装方法: crontab deploy/crontab.conf

# 每天凌晨 2 点自动备份数据库
0 2 * * * cd /opt/gongkao-v2 && ./deploy.sh backup >> /var/log/gongkao-backup.log 2>&1

# 每 5 分钟健康检查（可选）
# */5 * * * * curl -sf http://localhost/health > /dev/null || (cd /opt/gongkao-v2 && ./deploy.sh restart)
